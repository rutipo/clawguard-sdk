{
  "id": "clawguard-monitor",
  "endpoints": {
    "analyze": "/analyze-thread"
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": true,
    "properties": {
      "backendUrl": {
        "description": "ClawGuard backend URL. Optional if CLAWGUARD_BACKEND_URL is set."
      },
      "apiKey": {
        "description": "ClawGuard API key. Optional if CLAWGUARD_API_KEY is set."
      },
      "agentId": {
        "description": "Identifier for this agent. Optional if CLAWGUARD_AGENT_ID is set."
      },
      "captureFullIo": {
        "description": "Capture full tool input/output instead of summaries."
      },
      "maxFullIoBytes": {
        "description": "Maximum bytes captured when full I/O capture is enabled."
      },
      "blockSensitiveAccess": {
        "description": "Block access to sensitive files (.env, credentials, SSH keys)."
      },
      "requireApprovalForHighRisk": {
        "description": "Require user approval before potential data exfiltration."
      },
      "batchSize": {
        "description": "Number of events to buffer before flushing."
      },
      "flushIntervalMs": {
        "description": "Maximum time in milliseconds before buffered events are flushed."
      }
    }
  }
}
